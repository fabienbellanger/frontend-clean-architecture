{
  "name": "@frontend-clean-architecture/root",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/infrastructure/*"
  ],
  "scripts": {
    "postinstall": "lerna bootstrap",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^6.5.1"
  }
}
